{
  "name": "amortentia",
  "private": true,
  "packageManager": "npm@11.6.2",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:all": "bash scripts/dev-all.sh",
    "build": "npm run build:gelato-client && SKIP_ENV_CHECK=true npm run build -w @amortentia/storefront",
    "start": "turbo start",
    "lint": "turbo lint",
    "test": "turbo test",
    "backend:seed": "npm run seed:mvp -w @amortentia/backend",
    "backend:dev": "npm run dev -w @amortentia/backend",
    "storefront:dev": "npm run dev -w @amortentia/storefront",
    "build:gelato-client": "npm run build -w @amortentia/gelato-client"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "19.0.5",
      "@types/react-dom": "19.0.5"
    }
  },
  "devDependencies": {
    "next": "^15.5.18",
    "prettier": "^3.2.5",
    "turbo": "^2.0.14"
  },
  "workspaces": [
    "apps/**",
    "packages/*",
    "!apps/backend/.medusa/**"
  ],
  "overrides": {
    "ajv": "^8.0.0"
  }
}
